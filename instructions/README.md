# instructions/ - GenSpark AIエージェントからの指示フォルダ

このフォルダは、**GenSpark AIエージェント**がローカルPCの**CLIツール**（Claude Code、Codex CLI、Gemini CLI等）に指示を出す場所です。

## 🌐 GenSpark AIエージェント向け

### 📝 指示書の作成方法

1. **指示ファイルを作成**

```bash
# ファイル名の形式: YYYYMMDD-HHMM-<タスク名>.md
touch instructions/20260203-1500-implement-feature-x.md
```

2. **指示内容を記述**

```markdown
# タスク: 機能Xの実装

## 目的
機能Xを実装して、ユーザーがYできるようにする。

## 要件
- [ ] クラスAを作成
- [ ] メソッドBを実装
- [ ] テストを追加

## 実装ファイル
- src/main/xxx.js (新規)
- tests/unit/xxx.test.js (新規)

## 結果の出力先
results/20260203-1500-implement-feature-x-result.md

## 参考資料
- spec.md のセクション5を参照

## 期限
2026年2月5日まで

## 重要事項
- 必ず results/ フォルダに結果を出力すること
- 実装後は必ず git commit, git push すること
- CHANGELOG.md を更新すること
```

3. **GitHubにpush**

```bash
git add instructions/
git commit -m "task: Add instruction for feature X"
git push
```

4. **ローカルCLIツールの実行を待つ**

5. **結果を確認**

```bash
git pull
cat results/20260203-1500-implement-feature-x-result.md
```

## 💻 ローカルCLIツール向け

### 📥 指示の受け取り方

1. **最新の指示を取得**

```bash
git pull
```

2. **指示ファイルを確認**

```bash
ls instructions/
cat instructions/20260203-1500-implement-feature-x.md
```

3. **指示に従って実装**

4. **結果を results/ に出力**（指示書に書かれた出力先）

5. **変更をpush**

```bash
git add .
git commit -m "feat: Implement feature X"
git push
```

## 📋 テンプレート

```markdown
# タスク: [タスク名]

## 目的
[なぜこのタスクが必要か]

## 要件
- [ ] 要件1
- [ ] 要件2
- [ ] 要件3

## 実装ファイル
- path/to/file.js (新規/更新)

## 結果の出力先
results/YYYYMMDD-HHMM-taskname-result.md

## 制約条件
- 制約1
- 制約2

## 期限
[YYYY年MM月DD日まで]

## 参考資料
- [関連ドキュメント]
- [参考URL]

## 重要事項
- 必ず results/ フォルダに結果を出力すること
- 実装後は必ず git commit, git push すること
- CHANGELOG.md を更新すること

## 備考
[追加情報があれば]
```

## 🗂️ ファイル管理

### 完了したタスクの整理

```bash
# アーカイブフォルダを作成
mkdir -p instructions/archive/2026-02

# 完了した指示書を移動
git mv instructions/20260203-*-*.md instructions/archive/2026-02/
git commit -m "chore: Archive completed instructions"
git push
```

## 🚨 注意事項

- このフォルダは **Git管理に含まれます**（GenSparkとローカルCLIの連携のため）
- APIキーや機密情報は絶対に書かないこと
- 指示は具体的かつ明確に書くこと
- 結果の出力先を必ず指定すること
