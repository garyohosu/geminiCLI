# タスク: Phase 4 - 仕上げと配布準備（M0 100% 完成）

## 📋 タスク概要

M0 技術検証を完了させ、v1.0 リリースに向けた準備を整えます。Phase 4 では、E2Eテスト、高度な機能、配布準備、ドキュメント完成を行います。

## 🎯 目的

1. E2E テスト基盤の構築
2. 操作承認フロー（diff プレビュー）の実装
3. インストーラーの作成
4. ドキュメントの完成
5. M0 技術検証 100% 達成

## 📝 実装要件

Phase 4 は以下の4つのサブタスクに分割します：

---

## 🔧 サブタスク 1: E2E テスト基盤（優先度: 高）

### 目的
自動化されたE2Eテストで、アプリケーション全体の動作を保証します。

### 実装内容

#### 1. Playwright のセットアップ

```bash
npm install --save-dev @playwright/test playwright
```

#### 2. Playwright 設定ファイル (`playwright.config.js`)

```javascript
const { defineConfig } = require('@playwright/test');

module.exports = defineConfig({
  testDir: './tests/e2e',
  timeout: 30000,
  use: {
    headless: false,
    viewport: { width: 1200, height: 800 },
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    {
      name: 'electron',
      testMatch: /.*\.e2e\.test\.js$/,
    },
  ],
});
```

#### 3. E2E テストケース (`tests/e2e/app.e2e.test.js`)

必須テストケース：

```javascript
const { test, expect } = require('@playwright/test');
const { _electron: electron } = require('playwright');
const path = require('path');

let electronApp;
let window;

test.beforeAll(async () => {
  electronApp = await electron.launch({
    args: [path.join(__dirname, '../../src/main/index.js')]
  });
  window = await electronApp.firstWindow();
});

test.afterAll(async () => {
  await electronApp.close();
});

// テスト 1: アプリケーション起動
test('should launch application', async () => {
  expect(await window.title()).toBe('Gemini CLI GUI Wrapper');
});

// テスト 2: ワークスペース選択ボタンが表示される
test('should show workspace selection button', async () => {
  const button = await window.locator('#select-workspace-btn');
  expect(await button.isVisible()).toBe(true);
});

// テスト 3: Gemini CLI 制御ボタンが初期状態で無効
test('should have disabled control buttons initially', async () => {
  const startBtn = await window.locator('#start-btn');
  expect(await startBtn.isDisabled()).toBe(true);
});

// テスト 4: ワークスペース選択後、起動ボタンが有効化
// （注: この部分は実際のダイアログ操作が難しいため、モックが必要）

// テスト 5: 出力ログエリアが存在する
test('should have output log area', async () => {
  const outputLog = await window.locator('#output-log');
  expect(await outputLog.isVisible()).toBe(true);
});
```

#### 4. npm scripts の追加 (`package.json`)

```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:debug": "playwright test --debug"
  }
}
```

### 期待される成果

- ✅ Playwright が正常にセットアップされる
- ✅ 5+ の E2E テストケースが作成される
- ✅ すべてのテストがパスする
- ✅ CI で自動実行可能

---

## 🎨 サブタスク 2: 操作承認フロー（diff プレビュー）（優先度: 中）

### 目的
ファイル変更時に、変更内容をプレビューしてから実行できるようにします。

### 実装内容

#### 1. Diff ビューアーコンポーネント (`src/renderer/diff-viewer.js`)

```javascript
class DiffViewer {
  constructor() {
    this.modal = null;
    this.createModal();
  }

  createModal() {
    // モーダルダイアログを作成
    this.modal = document.createElement('div');
    this.modal.id = 'diff-modal';
    this.modal.className = 'modal hidden';
    this.modal.innerHTML = `
      <div class="modal-content">
        <h2>変更プレビュー</h2>
        <div id="diff-display"></div>
        <div class="modal-actions">
          <button id="approve-btn" class="btn-primary">承認して実行</button>
          <button id="cancel-btn" class="btn-secondary">キャンセル</button>
        </div>
      </div>
    `;
    document.body.appendChild(this.modal);
  }

  show(diffData) {
    // Diff を表示
    const diffDisplay = this.modal.querySelector('#diff-display');
    diffDisplay.innerHTML = this.formatDiff(diffData);
    this.modal.classList.remove('hidden');
  }

  formatDiff(diffData) {
    // 簡易的な diff 表示
    let html = '<pre class="diff">';
    diffData.changes.forEach(change => {
      if (change.type === 'add') {
        html += `<span class="diff-add">+ ${change.line}</span>\n`;
      } else if (change.type === 'remove') {
        html += `<span class="diff-remove">- ${change.line}</span>\n`;
      } else {
        html += `<span class="diff-context">  ${change.line}</span>\n`;
      }
    });
    html += '</pre>';
    return html;
  }

  hide() {
    this.modal.classList.add('hidden');
  }
}
```

#### 2. IPC ハンドラーの追加 (`src/main/index.js`)

```javascript
// ファイル書き込み前の diff 生成
ipcMain.handle('file:preview-write', async (event, filePath, newContent) => {
  try {
    const currentContent = await fileAPI.readText(filePath);
    const diff = generateDiff(currentContent, newContent);
    return { success: true, diff };
  } catch (error) {
    return { success: false, error: error.message };
  }
});

function generateDiff(oldContent, newContent) {
  const oldLines = oldContent.split('\n');
  const newLines = newContent.split('\n');
  const changes = [];
  
  // 簡易的な diff アルゴリズム
  const maxLen = Math.max(oldLines.length, newLines.length);
  for (let i = 0; i < maxLen; i++) {
    if (i >= oldLines.length) {
      changes.push({ type: 'add', line: newLines[i] });
    } else if (i >= newLines.length) {
      changes.push({ type: 'remove', line: oldLines[i] });
    } else if (oldLines[i] !== newLines[i]) {
      changes.push({ type: 'remove', line: oldLines[i] });
      changes.push({ type: 'add', line: newLines[i] });
    } else {
      changes.push({ type: 'context', line: oldLines[i] });
    }
  }
  
  return { changes };
}
```

#### 3. CSS の追加 (`src/renderer/style.css`)

```css
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal.hidden {
  display: none;
}

.modal-content {
  background: white;
  padding: 30px;
  border-radius: 8px;
  max-width: 800px;
  max-height: 80vh;
  overflow-y: auto;
}

.diff {
  background: #1e1e1e;
  color: #d4d4d4;
  padding: 15px;
  border-radius: 4px;
  font-family: 'Courier New', monospace;
  overflow-x: auto;
}

.diff-add {
  color: #4ec9b0;
  background: rgba(78, 201, 176, 0.2);
}

.diff-remove {
  color: #f48771;
  background: rgba(244, 135, 113, 0.2);
}

.modal-actions {
  margin-top: 20px;
  display: flex;
  gap: 10px;
  justify-content: flex-end;
}
```

### 期待される成果

- ✅ ファイル変更時に diff プレビューが表示される
- ✅ ユーザーが承認してから実行される
- ✅ キャンセル機能が動作する

---

## 📦 サブタスク 3: インストーラー作成（優先度: 中）

### 目的
Windows ユーザーが簡単にインストールできるインストーラーを作成します。

### 実装内容

#### 1. electron-builder のセットアップ

```bash
npm install --save-dev electron-builder
```

#### 2. package.json の更新

```json
{
  "build": {
    "appId": "com.gemini.cli.gui",
    "productName": "Gemini CLI GUI Wrapper",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": ["nsis", "portable"],
      "icon": "build/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "files": [
      "src/**/*",
      "package.json"
    ]
  },
  "scripts": {
    "build": "electron-builder --win",
    "build:portable": "electron-builder --win portable"
  }
}
```

#### 3. アイコンの作成

- `build/icon.ico` (256x256 PNG から変換)
- `build/icon.png` (元画像)

#### 4. ビルド実行

```bash
npm run build
```

### 期待される成果

- ✅ インストーラー (.exe) が生成される
- ✅ ポータブル版 (.exe) が生成される
- ✅ インストール/アンインストールが正常に動作する

---

## 📚 サブタスク 4: ドキュメント完成（優先度: 中）

### 目的
ユーザーと開発者向けの包括的なドキュメントを作成します。

### 作成するドキュメント

#### 1. ユーザーガイド (`docs/user-guide.md`)

```markdown
# Gemini CLI GUI Wrapper ユーザーガイド

## インストール

1. [Releases](https://github.com/garyohosu/geminiCLI/releases) から最新版をダウンロード
2. インストーラーを実行
3. インストールウィザードに従う

## 基本的な使い方

### ワークスペースの選択

1. アプリを起動
2. 「ワークスペースを選択」ボタンをクリック
3. 作業したいフォルダを選択

### Gemini CLI の起動

1. ワークスペース選択後、「Gemini CLI 起動」ボタンをクリック
2. ステータスが「実行中」になるまで待つ

### メッセージの送信

1. チャット入力欄にメッセージを入力
2. 「送信」ボタンをクリック
3. 出力ログでレスポンスを確認

## トラブルシューティング

### Gemini CLI が起動しない

- Gemini CLI がインストールされているか確認
- ワークスペースのパスが正しいか確認

### 出力が表示されない

- ブラウザをリフレッシュ（Ctrl+R）
- アプリを再起動
```

#### 2. 開発者ガイド (`docs/developer-guide.md`)

開発環境のセットアップ、アーキテクチャ、コントリビューション方法を記載。

#### 3. API ドキュメント (`docs/api.md`)

IPC API、FileAPI、GeminiCLIManager の使用方法を記載。

#### 4. README.md の更新

スクリーンショット、インストール方法、ビルド方法を追加。

### 期待される成果

- ✅ ユーザーガイドが完成
- ✅ 開発者ガイドが完成
- ✅ API ドキュメントが完成
- ✅ README.md が充実

---

## 📊 期待される最終成果物

### ファイル一覧

```
新規作成:
- playwright.config.js
- tests/e2e/app.e2e.test.js
- src/renderer/diff-viewer.js
- build/icon.ico
- build/icon.png
- docs/user-guide.md
- docs/developer-guide.md
- docs/api.md

更新:
- package.json (scripts, build 設定)
- src/main/index.js (diff プレビュー IPC)
- src/renderer/style.css (モーダル CSS)
- README.md (スクリーンショット、インストール方法)
```

### 統計

- **E2E テストケース**: 5+
- **インストーラー**: Windows NSIS + Portable
- **ドキュメント**: 3ファイル (User Guide, Developer Guide, API)
- **総ドキュメント量**: 10KB+

---

## 🧪 テスト要件

### E2E テスト

```bash
npm run test:e2e
```

すべてのテストがパスすること。

### インストーラーテスト

- Windows 10/11 でインストール実行
- アプリが正常に起動
- アンインストールが正常に完了

### マニュアルテスト

- diff プレビュー機能の動作確認
- 承認フローの確認
- キャンセル機能の確認

---

## 📝 結果の報告

作業完了後、以下のファイルに結果を記録してください：

```
results/20260203-0450-phase4-complete-result.md
```

### テンプレート

```markdown
# タスク結果: Phase 4 - 仕上げと配布準備

## ✅ ステータス: [成功 / 一部成功 / 失敗]

## 📊 実装結果

### サブタスク 1: E2E テスト
- Playwright セットアップ: [完了 / 未完了]
- E2E テストケース数: XX個
- テスト結果: [XX passed / XX failed]

### サブタスク 2: 操作承認フロー
- Diff ビューアー: [完了 / 未完了]
- IPC ハンドラー: [完了 / 未完了]
- 動作確認: [成功 / 失敗]

### サブタスク 3: インストーラー
- electron-builder セットアップ: [完了 / 未完了]
- インストーラー生成: [成功 / 失敗]
- インストールテスト: [成功 / 失敗]

### サブタスク 4: ドキュメント
- ユーザーガイド: [完了 / 未完了]
- 開発者ガイド: [完了 / 未完了]
- API ドキュメント: [完了 / 未完了]

## 📈 進捗

M0 技術検証: [XX]% 完了

## ❗ 問題点・課題

（あれば記載）

## ⏱️ 所要時間

約 XX 時間

## 📝 備考

（その他気づいた点があれば記載）
```

---

## 🎯 完成基準

### M0 技術検証 100% 達成の条件

- ✅ E2E テストが 5+ 個作成され、すべてパス
- ✅ Diff プレビュー機能が動作
- ✅ Windows インストーラーが正常に生成
- ✅ ユーザーガイド、開発者ガイド、API ドキュメントが完成
- ✅ README.md にスクリーンショットとインストール手順が追加
- ✅ すべての変更が GitHub にプッシュ
- ✅ CHANGELOG.md が更新

---

## ⏰ 期限

1週間以内（緊急度: 中）

---

## 🔗 参考リンク

- [Playwright for Electron](https://playwright.dev/docs/api/class-electron)
- [electron-builder](https://www.electron.build/)
- [Diff アルゴリズム](https://en.wikipedia.org/wiki/Diff)

---

**Note**: Phase 4 の完成により、**M0 技術検証が 100% 完了**し、v1.0 リリースの準備が整います！
