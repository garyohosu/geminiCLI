# Gemini CLI GUI Wrapper - 開発者ガイド

**バージョン**: 1.0.0  
**最終更新**: 2026-02-03

---

## 📖 目次

1. [開発環境のセットアップ](#開発環境のセットアップ)
2. [プロジェクト構造](#プロジェクト構造)
3. [アーキテクチャ](#アーキテクチャ)
4. [開発ワークフロー](#開発ワークフロー)
5. [テスト](#テスト)
6. [ビルドとリリース](#ビルドとリリース)
7. [コントリビューション](#コントリビューション)

---

## 開発環境のセットアップ

### 必須ツール

- **Node.js**: 18.x 以上
- **npm**: 9.x 以上
- **Git**: 最新版
- **Visual Studio Code**（推奨）

### クローンとインストール

```bash
# リポジトリをクローン
git clone https://github.com/garyohosu/geminiCLI.git
cd geminiCLI

# 依存関係をインストール
npm install

# 開発モードで起動
npm run dev
```

### 開発用コマンド

```bash
# アプリを起動（通常モード）
npm start

# アプリを起動（DevTools 表示）
npm run dev

# ユニットテストを実行
npm test

# ウォッチモードでテスト
npm run test:watch

# カバレッジレポートを生成
npm run test:coverage

# E2Eテストを実行
npm run test:e2e

# リンターを実行
npm run lint

# コードフォーマット
npm run format

# ビルド（Windows インストーラー）
npm run build
```

---

## プロジェクト構造

```
gemini-cli-gui-wrapper/
├── src/
│   ├── main/                    # Electron Main プロセス
│   │   ├── index.js             # エントリーポイント
│   │   ├── gemini-cli-manager.js # Gemini CLI 管理
│   │   ├── path-validator.js    # パス検証
│   │   └── file-api.js          # ファイル操作 API
│   ├── preload/                 # Preload スクリプト
│   │   └── preload.js           # contextBridge API
│   └── renderer/                # Renderer プロセス（UI）
│       ├── index.html           # メイン HTML
│       ├── renderer.js          # UI ロジック
│       └── style.css            # スタイルシート
├── tests/
│   ├── unit/                    # ユニットテスト
│   │   ├── path-validator.test.js
│   │   ├── file-api.test.js
│   │   └── gemini-cli-manager.test.js
│   ├── e2e/                     # E2E テスト
│   │   └── app.e2e.test.js
│   └── security/                # セキュリティテスト
├── docs/                        # ドキュメント
│   ├── user-guide.md
│   ├── developer-guide.md
│   ├── api.md
│   └── architecture.md
├── instructions/                # AI → 人間の指示
├── results/                     # 人間 → AI の結果
├── build/                       # ビルド用アセット
│   ├── icon.ico
│   └── icon.png
├── package.json                 # プロジェクト設定
├── jest.config.js               # Jest 設定
├── playwright.config.js         # Playwright 設定
└── README.md                    # プロジェクト README
```

---

## アーキテクチャ

### 概要図

```
┌─────────────────────────────────────────────────────────┐
│                     Renderer Process                    │
│                                                           │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │  index.html │  │ renderer.js  │  │   style.css   │  │
│  └─────────────┘  └──────────────┘  └───────────────┘  │
│                           │                              │
│                           │ IPC (contextBridge)          │
│                           ▼                              │
└───────────────────────────────────────────────────────┘
                             │
                             │
┌───────────────────────────────────────────────────────┐
│                   Preload Script                       │
│                                                         │
│  ┌───────────────────────────────────────────────┐    │
│  │           contextBridge.exposeInMainWorld     │    │
│  │  - selectWorkspace()                          │    │
│  │  - gemini.start(), gemini.stop(), ...         │    │
│  │  - files.list(), files.read(), ...            │    │
│  └───────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────┘
                             │
                             │ IPC Handlers
                             ▼
┌────────────────────────────────────────────────────────┐
│                     Main Process                       │
│                                                         │
│  ┌──────────────┐  ┌─────────────────┐  ┌──────────┐ │
│  │  index.js    │  │ GeminiCLIManager│  │ FileAPI  │ │
│  │  (IPC Hub)   │──│  (subprocess)   │  │ (secure) │ │
│  └──────────────┘  └─────────────────┘  └──────────┘ │
│         │                   │                  │       │
│         │                   ▼                  │       │
│         │          PathValidator               │       │
│         │           (security)                 │       │
│         └──────────────────┬──────────────────┘       │
│                            │                            │
└────────────────────────────────────────────────────────┘
                             │
                             ▼
                     ┌──────────────┐
                     │  Gemini CLI  │
                     │  (external)  │
                     └──────────────┘
```

### 主要コンポーネント

#### 1. Main プロセス (`src/main/index.js`)

- **役割**: アプリケーションのエントリーポイント、ウィンドウ管理、IPC ハンドラー
- **責務**:
  - BrowserWindow の作成と管理
  - IPC 通信のルーティング
  - GeminiCLIManager と FileAPI の調整
  - イベントの転送（Gemini CLI → Renderer）

#### 2. GeminiCLIManager (`src/main/gemini-cli-manager.js`)

- **役割**: Gemini CLI の子プロセス管理
- **責務**:
  - プロセスの起動・停止・再起動
  - stdin/stdout/stderr の管理
  - イベントエミッター（stdout, stderr, error, close など）
  - 自動再起動機能

#### 3. PathValidator (`src/main/path-validator.js`)

- **役割**: パス検証とセキュリティ
- **責務**:
  - ワークスペース境界の強制
  - パストラバーサル攻撃の防止
  - シンボリックリンク脱出の防止
  - Windows/Unix パスの正規化

#### 4. FileAPI (`src/main/file-api.js`)

- **役割**: 安全なファイル操作
- **責務**:
  - PathValidator を経由したファイル操作
  - list, read, write, move, copy, delete など
  - 再帰的ディレクトリ操作
  - ファイル検索

#### 5. Preload スクリプト (`src/preload/preload.js`)

- **役割**: Main と Renderer の安全なブリッジ
- **責務**:
  - contextBridge による API 公開
  - セキュリティ境界の維持
  - イベントリスナーの管理

#### 6. Renderer プロセス (`src/renderer/`)

- **役割**: ユーザーインターフェース
- **責務**:
  - UI の表示と更新
  - ユーザー入力の処理
  - Preload API の呼び出し
  - 出力ログの表示

---

## 開発ワークフロー

### AI-Human 協働開発

このプロジェクトは **GenSpark AI** と **ローカルCLI**（Claude Code / Gemini CLI）の協働で開発されます。

#### フロー

1. **GenSpark AI**: `instructions/` に指示書を作成して `git push`
2. **ローカルPC**: `git pull` して指示を確認
3. **ローカルCLI**: 実装とテスト
4. **ローカルPC**: `results/` に結果を記録して `git push`
5. **GenSpark AI**: `git pull` して結果を確認、次の指示を作成

#### 指示ファイルの命名規則

```
instructions/YYYYMMDD-HHMM-<タスク名>.md
results/YYYYMMDD-HHMM-<タスク名>-result.md
```

### ブランチ戦略

- **main**: 安定版（常にリリース可能な状態）
- **develop**: 開発中（機能ブランチから統合）
- **feature/xxx**: 機能開発用
- **bugfix/xxx**: バグ修正用

### コミットメッセージ

Conventional Commits 形式を使用：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type**:
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメント
- `style`: コードスタイル
- `refactor`: リファクタリング
- `test`: テスト
- `chore`: ビルド・設定

**例**:
```
feat(gemini): Add auto-restart functionality

- Implement auto-restart option
- Add maxRestarts configuration
- Update tests

Refs: #M0-Phase2
```

---

## テスト

### ユニットテスト（Jest）

```bash
# すべてのユニットテストを実行
npm test

# ウォッチモード
npm run test:watch

# カバレッジレポート
npm run test:coverage
```

#### テストの書き方

```javascript
// tests/unit/example.test.js
const MyModule = require('../../src/main/my-module');

describe('MyModule', () => {
  describe('myFunction', () => {
    it('should return expected value', () => {
      const result = MyModule.myFunction('input');
      expect(result).toBe('expected');
    });

    it('should throw error for invalid input', () => {
      expect(() => {
        MyModule.myFunction(null);
      }).toThrow('Invalid input');
    });
  });
});
```

### E2E テスト（Playwright）

```bash
# E2E テストを実行
npm run test:e2e

# デバッグモード
npm run test:e2e:debug
```

#### E2E テストの書き方

```javascript
// tests/e2e/example.e2e.test.js
const { test, expect } = require('@playwright/test');
const { _electron: electron } = require('playwright');

test('should do something', async () => {
  const app = await electron.launch({ args: ['src/main/index.js'] });
  const window = await app.firstWindow();
  
  // テストロジック
  const button = await window.locator('#my-button');
  await button.click();
  
  expect(await window.title()).toBe('Expected Title');
  
  await app.close();
});
```

### セキュリティテスト

パストラバーサル、シンボリックリンク脱出などの攻撃パターンをテスト。

```javascript
// tests/unit/path-validator.test.js
describe('PathValidator - Attack Patterns', () => {
  it('should block path traversal', () => {
    expect(() => {
      validator.validate('../../../etc/passwd');
    }).toThrow('PATH_OUTSIDE_WORKSPACE');
  });
});
```

---

## ビルドとリリース

### ビルドの作成

```bash
# Windows インストーラーをビルド
npm run build

# ポータブル版をビルド
npm run build:portable
```

生成物: `dist/` ディレクトリ

### リリース手順

1. **バージョンアップ**
   ```bash
   npm version patch  # 1.0.0 → 1.0.1
   # または
   npm version minor  # 1.0.0 → 1.1.0
   # または
   npm version major  # 1.0.0 → 2.0.0
   ```

2. **CHANGELOG.md の更新**
   ```markdown
   ## [1.1.0] - 2026-02-10
   
   ### Added
   - New feature X
   
   ### Fixed
   - Bug Y
   ```

3. **ビルド**
   ```bash
   npm run build
   ```

4. **GitHub Release 作成**
   - タグを作成: `v1.1.0`
   - リリースノートを記載
   - ビルド成果物をアップロード

5. **プッシュ**
   ```bash
   git push origin main --tags
   ```

---

## コントリビューション

### プルリクエストのフロー

1. Fork リポジトリを作成
2. Feature ブランチを作成
   ```bash
   git checkout -b feature/my-feature
   ```
3. 変更を実装
4. テストを追加・実行
   ```bash
   npm test
   npm run test:e2e
   ```
5. コミット
   ```bash
   git commit -m "feat: Add my feature"
   ```
6. プッシュ
   ```bash
   git push origin feature/my-feature
   ```
7. GitHub で Pull Request を作成

### コードレビューのポイント

- ✅ すべてのテストがパス
- ✅ コードスタイルが統一されている
- ✅ ドキュメントが更新されている
- ✅ セキュリティ上の問題がない
- ✅ パフォーマンスが劣化していない

---

## デバッグ

### Main プロセスのデバッグ

```bash
# --inspect フラグで起動
npm start -- --inspect

# Chrome DevTools を開く
# chrome://inspect で接続
```

### Renderer プロセスのデバッグ

```bash
# 開発モードで起動（DevTools 自動表示）
npm run dev

# または Ctrl+Shift+I で DevTools を開く
```

### ログの確認

```javascript
// Main プロセス
console.log('Main log');

// Renderer プロセス
console.log('Renderer log');
```

---

## 参考リンク

- [Electron ドキュメント](https://www.electronjs.org/docs/latest/)
- [Jest ドキュメント](https://jestjs.io/docs/getting-started)
- [Playwright ドキュメント](https://playwright.dev/docs/intro)
- [Conventional Commits](https://www.conventionalcommits.org/)

---

**Happy Coding! 🚀**
