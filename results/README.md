# results/ - ローカルCLIツールからの結果報告フォルダ

このフォルダは、ローカルPCの**CLIツール**（Claude Code、Codex CLI、Gemini CLI等）が**GenSpark AIエージェント**にタスク実行結果を報告する場所です。

## 💻 ローカルCLIツール向け

### 📤 結果の出力方法

1. **instructions/ の指示書を読む**

```bash
git pull
cat instructions/20260203-1500-implement-feature-x.md
```

指示書に書かれた「結果の出力先」を確認：
```markdown
## 結果の出力先
results/20260203-1500-implement-feature-x-result.md
```

2. **タスクを実行**（実装・テスト）

3. **結果ファイルを作成**

```bash
# 指示書に書かれた出力先に結果を作成
cat > results/20260203-1500-implement-feature-x-result.md << 'EOF'
# タスク結果: 機能Xの実装

## ステータス
✅ 完了

## 実装内容
- クラスAを作成しました
- メソッドBを実装しました

## 変更ファイル
- 新規: src/main/feature-x.js (120行)
- 新規: tests/unit/feature-x.test.js (50行)

## テスト結果
✅ すべてのテストが合格 (15/15)

## コミット情報
- コミットハッシュ: abc1234
- コミットメッセージ: "feat: Implement feature X"

## 次のステップ
1. 統合テストの追加
2. ドキュメント更新

## 問題点・課題
なし

## 所要時間
約1時間
EOF
```

4. **変更をpush**

```bash
git add .
git commit -m "feat: Implement feature X"
git push
```

## 🌐 GenSpark AIエージェント向け

### 🔍 結果の確認方法

1. **最新の結果を取得**

```bash
git pull
```

2. **結果ファイルを確認**

```bash
cat results/20260203-1500-implement-feature-x-result.md
```

3. **ステータスを確認**

```markdown
## ステータス
✅ 完了 / ⚠️ 部分完了 / ❌ 未完了
```

4. **次のアクションを決定**

- **✅ 完了**: 次のタスクの指示を instructions/ に作成
- **⚠️ 部分完了**: 追加の指示を出すか、修正を依頼
- **❌ 未完了**: 問題点を確認し、指示を修正

## 📋 結果ファイルのテンプレート

```markdown
# タスク結果: [タスク名]

## ステータス
✅ 完了 / ⚠️ 部分完了 / ❌ 未完了

## 実装内容
- [実装した機能の説明]

## 変更ファイル
- 新規: path/to/file.js (行数)
- 更新: path/to/existing.js (変更内容)
- 削除: path/to/old.js

## テスト結果
✅ すべてのテストが合格 (XX/XX)
または
❌ テスト失敗 (XX/XX) - エラー内容

## コミット情報
- コミットハッシュ: [hash]
- コミットメッセージ: "[message]"

## 次のステップ（推奨）
1. [次にやるべきこと]
2. [次にやるべきこと]

## 問題点・課題
- [発生した問題や疑問点]
- [GenSpark AIエージェントへの質問]

## 所要時間
約XX分

## 備考
[その他の情報]
```

## 🗂️ ファイル管理

### 古い結果のアーカイブ

```bash
# アーカイブフォルダを作成
mkdir -p results/archive/2026-02

# 完了した結果を移動
git mv results/20260203-*-result.md results/archive/2026-02/
git commit -m "chore: Archive completed results"
git push
```

## 📊 結果の分析

### 成功率の確認

```bash
# 完了したタスク数をカウント
grep -l "✅ 完了" results/*.md | wc -l

# 失敗したタスク数をカウント
grep -l "❌ 未完了" results/*.md | wc -l
```

### 平均所要時間の確認

```bash
# 所要時間を抽出
grep "所要時間" results/*.md
```

## 🚨 注意事項

- このフォルダは **Git管理に含まれます**（GenSparkとローカルCLIの連携のため）
- 結果ファイルは必ず生成すること（失敗した場合も）
- APIキーや機密情報は絶対に書かないこと
- ステータスは正確に記載すること
- 問題点は詳細に記述すること（GenSparkが改善できるように）
- 必ず git push すること（GenSparkが確認できるように）
